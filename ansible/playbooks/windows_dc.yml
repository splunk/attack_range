- hosts: all
  gather_facts: False
#  tasks:
#    - debug:
#        msg: "{{ hostvars[inventory_hostname] }}"
  roles:
    - common
    - windows_dns_server
    - windows_domain_controller
    - universal_forwarder
    - sysmon
  vars_files:
    - ../vars/vars.yml 
